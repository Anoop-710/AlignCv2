// import { calculateMatchScore } from "./utils";

// export async function processRestructure(
//   resumeText: string,
//   jobDescription: string
// ): Promise<{ score: number; output: string }> {
//   // Mock logic: use random score and placeholder output
//   const score = calculateMatchScore(resumeText, jobDescription);
//   const output = `Restructured Resume (mock):\n\n${resumeText.slice(
//     0,
//     200
//   )}...`;
//   return { score, output };
// }
